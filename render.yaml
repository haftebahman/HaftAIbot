services:
  - type: worker
    name: telegram-grok-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: XAI_API_KEY
        sync: false
      - key: ADMIN_ID
        sync: false
